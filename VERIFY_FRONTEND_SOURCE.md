# 🔍 Проверка: Откуда загружается Frontend

## Как проверить

### В браузере (Developer Tools):

1. Открой `https://dashboard.headcorn.pro`
2. Нажми `F12` (открыть Developer Tools)
3. Перейди на вкладку **Network**
4. Обнови страницу (`Ctrl+R` или `F5`)
5. Найди запрос к `index.html` или `index-*.js`
6. Посмотри на **URL** запроса:
   - Если `dashboard.headcorn.pro` → frontend с Beget ✅
   - Если `dashboard-frontend.onrender.com` → frontend с Render ❌

---

## Если frontend загружается с Render

**Нужно отключить frontend на Render:**

1. В Render Dashboard найди сервис `dashboard-frontend` (если есть)
2. Удали его или останови

---

## Если frontend загружается с Beget, но изменения не видны

### Проблема 1: Кеш браузера

**Решение:**
1. Нажми `Ctrl+Shift+R` (жесткая перезагрузка)
2. Или `Ctrl+F5`
3. Или очисти кеш браузера

### Проблема 2: Старые файлы на Beget

**Проверь на сервере:**

```bash
# Войди на Beget через SSH
cd ~/dashboard.headcorn.pro/public_html

# Проверь дату изменения файлов
ls -la index.html
ls -la assets/

# Должны быть свежие (сегодняшние)
```

**Если старые, загрузи заново через FileZilla.**

### Проблема 3: Backend не возвращает колонку "Изменение"

**Проверь в браузере (Console):**

1. Открой `F12` → Console
2. Найди запрос к `/api/dashboard/`
3. Посмотри Response
4. Найди блок с `id: "approval_time"`
5. Проверь, есть ли колонка "Изменение" в `columns`

---

## Быстрая проверка

**В браузере выполни в Console:**

```javascript
// Проверь, какой API URL используется
console.log('API URL:', window.location.origin);

// Проверь данные дашборда
fetch('/api/dashboard/')
  .then(r => r.json())
  .then(data => {
    const approvalItem = data.items.find(i => i.id === 'approval_time');
    console.log('Approval time columns:', approvalItem?.columns);
    console.log('Approval time data:', approvalItem?.data);
  });
```

**Пришли результаты.**




