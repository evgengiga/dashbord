# 🚀 Обновление дашборда на Render

## 📋 Что изменилось

### 1. **Имя пользователя вместо email**
- Теперь в шапке дашборда отображается полное имя пользователя (ФИО), а не email
- Planfix API возвращает компоненты имени (фамилия, имя, отчество), которые объединяются в полное ФИО

### 2. **Исправлен SQL-запрос**
- ✅ Добавлена проверка **двух таблиц**: `proscheti_gr_artema` и `proscheti_gr_zheni`
- ✅ Фильтрация по столбцу `"user"` с ФИО залогиненного пользователя
- ✅ Добавлен столбец **"Период"** с тремя фиксированными строками:
  - **Текущий квартал**
  - **Прошлый квартал**
  - **Финансовый год** (март - февраль)
- ✅ Убран столбец "Менеджер" из таблицы (имя отображается в шапке)
- ✅ Добавлена детальная отладка в логах

### 3. **Выбор финансового года**
- Добавлен выпадающий список для выбора финансового года
- Можно переключаться между **текущим** и **прошлым** финансовым годом
- Финансовый год: **1 марта - 28 февраля**

## 🔧 Шаги для деплоя

### 1. Коммит и пуш изменений

```bash
cd C:\Users\dante\OneDrive\Desktop\test-cursor

git add .
git commit -m "Fix: Display user name, add fiscal year selector, fix SQL queries"
git push origin main
```

### 2. Деплой на Render

#### Backend (автоматически задеплоится):
1. Зайдите на https://dashboard.render.com
2. Найдите ваш backend сервис
3. После пуша в Git, Render автоматически начнет деплой
4. Дождитесь завершения (статус: "Live")

#### Frontend (автоматически задеплоится):
1. Найдите ваш frontend сервис
2. После пуша в Git, Render автоматически пересоберет статику
3. Дождитесь завершения

### 3. Проверка логов

После деплоя проверьте логи backend на Render:

```
🔍 Executing conversions query for user: 'Фамилия Имя Отчество'
✅ Query executed, rows returned: 3
📊 Sample row: {'Период': 'Текущий квартал', ...}
```

## 🐛 Что проверить после деплоя

1. **Логин** - убедитесь, что логин работает
2. **Имя в шапке** - должно отображаться "Привет, Фамилия Имя Отчество! 👋"
3. **Таблица с данными** - должны быть 3 строки:
   - Текущий квартал
   - Прошлый квартал
   - Финансовый год
4. **Данные в таблице** - проверьте, что цифры не нулевые (если есть данные в БД)
5. **Выбор финансового года** - переключите между текущим и прошлым годом

## 🔍 Если таблица пустая (все нули)

Проверьте логи Render backend:
1. Найдите строку: `🔍 Executing conversions query for user: '...'`
2. Убедитесь, что **ФИО точно совпадает** со значением в столбце `"user"` в базе данных
3. Если ФИО не совпадает - проверьте данные в Planfix API (логи покажут)

### Возможные несовпадения:

- **В БД**: `"Иванов Иван Иванович"`
- **Из Planfix**: `"Иван Иванов Иванович"` ❌ (порядок слов)
- **Из Planfix**: `"Иванов  Иван  Иванович"` ❌ (двойные пробелы)
- **Из Planfix**: `"Иванов Иван"` ❌ (нет отчества)

## 📊 Структура финансового года

**Финансовый год** = с 1 марта по 28 февраля

Примеры:
- Если сейчас **декабрь 2025**, то:
  - **Текущий финансовый год**: 1 марта 2025 - 28 февраля 2026
  - **Прошлый финансовый год**: 1 марта 2024 - 28 февраля 2025

- Если сейчас **февраль 2026**, то:
  - **Текущий финансовый год**: 1 марта 2025 - 28 февраля 2026
  - **Прошлый финансовый год**: 1 марта 2024 - 28 февраля 2025

- Если сейчас **март 2026**, то:
  - **Текущий финансовый год**: 1 марта 2026 - 28 февраля 2027
  - **Прошлый финансовый год**: 1 марта 2025 - 28 февраля 2026

## 🆘 Если что-то не работает

### Проблема: Имя не отображается (показывается email)
**Решение**: Проверьте логи backend Planfix API response - должны быть поля `surname`, `name`, `patronymic`

### Проблема: Таблица пустая (все нули)
**Решение**:
1. Проверьте, что в БД есть данные для вашего пользователя
2. Проверьте точное совпадение ФИО в логах и в БД
3. Проверьте, что столбец называется именно `"user"` (с кавычками если есть регистр)

### Проблема: Финансовый год не переключается
**Решение**: 
1. Откройте DevTools в браузере (F12)
2. Вкладка Network - должен быть запрос к `/api/dashboard/?fiscal_year=previous` или `current`
3. Если запроса нет - очистите кэш браузера (Ctrl+Shift+R)

## ✅ Готово!

После успешного деплоя ваш дашборд должен:
- ✅ Показывать ваше полное имя
- ✅ Отображать данные только по вашему пользователю
- ✅ Показывать 3 периода с данными
- ✅ Позволять переключаться между финансовыми годами

