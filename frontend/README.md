# Frontend - Dashboard Service

React приложение для персонализированного дашборда.

## Установка

### 1. Установите Node.js

Убедитесь, что у вас установлен Node.js версии 18+ и npm:

```bash
node --version
npm --version
```

### 2. Установите зависимости

```bash
cd frontend
npm install
```

## Запуск

### Development режим

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

### Production сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Preview production сборки

```bash
npm run preview
```

## Структура

```
frontend/
├── src/
│   ├── components/       # React компоненты
│   │   ├── DataTable.jsx # Компонент таблицы данных
│   │   └── DataTable.css
│   ├── pages/           # Страницы приложения
│   │   ├── LoginPage.jsx       # Страница входа
│   │   ├── LoginPage.css
│   │   ├── DashboardPage.jsx   # Страница дашборда
│   │   └── DashboardPage.css
│   ├── services/        # API клиенты
│   │   └── api.js       # Axios клиент для backend API
│   ├── App.jsx          # Главный компонент
│   ├── App.css
│   ├── main.jsx         # Точка входа
│   └── index.css        # Глобальные стили
├── index.html           # HTML шаблон
├── vite.config.js       # Конфигурация Vite
├── package.json         # Зависимости
└── README.md
```

## Основные компоненты

### LoginPage
- Форма входа по email
- Валидация email
- Обработка ошибок
- Красивый UI с анимациями

### DashboardPage
- Отображение данных пользователя
- Список дашбордов
- Компонент таблиц
- Кнопка выхода

### DataTable
- Отображение табличных данных
- Автоматическое форматирование
- Цветовая индикация для процентов:
  - 🟢 Зеленый: ≥70%
  - 🟡 Желтый: 40-69%
  - 🟠 Оранжевый: <40%
- Responsive дизайн
- Горизонтальный скролл для больших таблиц

## API

Frontend взаимодействует с backend API через `src/services/api.js`:

- **POST /api/auth/login** - Вход по email
- **GET /api/auth/me** - Получение данных текущего пользователя
- **GET /api/dashboard/** - Получение данных дашборда

Все запросы автоматически включают JWT токен из localStorage.

## Настройка API URL

По умолчанию используется `http://localhost:8000/api`.

Для изменения отредактируйте `src/services/api.js`:

```javascript
const API_BASE_URL = 'http://your-backend-url/api'
```

Или используйте переменные окружения (создайте `.env`):

```env
VITE_API_URL=http://your-backend-url/api
```

И в коде:

```javascript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api'
```

## Стилизация

Приложение использует чистый CSS с современными возможностями:
- CSS Grid и Flexbox
- CSS Variables
- Animations и Transitions
- Responsive дизайн

Цветовая схема:
- Основной: #667eea → #764ba2 (градиент)
- Успех: #4CAF50
- Фон: #f5f7fa
- Текст: #333

## Особенности

### Аутентификация
- JWT токены хранятся в localStorage
- Автоматическое добавление токена ко всем запросам
- Редирект на логин при истечении токена

### Персонализация
- Все данные фильтруются по ФИО пользователя
- Отображение имени пользователя в header
- Персонализированное приветствие

### UX
- Loading состояния
- Обработка ошибок
- Красивые анимации
- Responsive дизайн для мобильных устройств

## Разработка

### Добавление нового дашборда

Backend автоматически возвращает список дашбордов. Frontend отображает их автоматически.

Просто добавьте новый запрос в `backend/app/services/dashboard_service.py`.

### Кастомизация таблиц

Отредактируйте `src/components/DataTable.jsx` для изменения:
- Форматирования значений
- Цветовых индикаторов
- Стилей

### Добавление новых страниц

1. Создайте компонент в `src/pages/`
2. Добавьте роутинг в `src/App.jsx`
3. Создайте соответствующие API методы в `src/services/api.js`

## Troubleshooting

### CORS ошибки

Убедитесь, что backend настроен на разрешение запросов с `http://localhost:5173`.

Проверьте `backend/.env`:
```env
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
```

### API недоступен

Убедитесь, что backend запущен на `http://localhost:8000`.

### Таблицы не отображаются

Проверьте, что SQL-запросы в backend правильно настроены и возвращают данные.

## Browser Support

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Production Deploy

### Сборка

```bash
npm run build
```

### Деплой статических файлов

Файлы из папки `dist/` можно развернуть на:
- Nginx
- Apache
- Netlify
- Vercel
- GitHub Pages

Убедитесь, что обновили `API_BASE_URL` на production URL backend.




