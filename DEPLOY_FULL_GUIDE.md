# 🚀 Полная инструкция по развертыванию дашборда в интернете

## 📋 Что нужно перед началом

1. ✅ **GitHub аккаунт** (если еще нет: https://github.com/signup)
2. ✅ **Render аккаунт** (бесплатный: https://render.com)
3. ✅ **Проект уже загружен на GitHub** (у вас уже есть: https://github.com/evgengiga/dashbord)

---

## 🎯 Шаг 1: Подготовка проекта на GitHub

### 1.1. Проверьте, что все изменения закоммичены

```bash
cd C:\Users\dante\OneDrive\Desktop\test-cursor
git status
```

Если есть незакоммиченные изменения:
```bash
git add -A
git commit -m "Prepare for deployment"
git push origin main
```

### 1.2. Убедитесь, что репозиторий публичный или у вас есть доступ

Ваш репозиторий: `https://github.com/evgengiga/dashbord`

---

## 🎯 Шаг 2: Регистрация на Render

1. Откройте: **https://render.com**
2. Нажмите **"Get Started for Free"**
3. Выберите **"Sign up with GitHub"**
4. Авторизуйте Render доступ к вашему GitHub аккаунту
5. Подтвердите email (проверьте почту)

---

## 🎯 Шаг 3: Деплой Backend (Python API)

### 3.1. Создайте Web Service

1. В Render Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите **"Connect account"** → выберите ваш GitHub аккаунт
3. Выберите репозиторий: **`evgengiga/dashbord`**
4. Нажмите **"Connect"**

### 3.2. Настройте Backend

Заполните следующие поля:

- **Name:** `dashboard-backend` (или любое другое имя)
- **Region:** `Frankfurt (EU)` (или ближайший к вам)
- **Branch:** `main`
- **Root Directory:** `backend` ⚠️ **ВАЖНО!**
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
- **Plan:** `Free` (или `Starter` за $7/мес, если нужна стабильность)

### 3.3. Добавьте Environment Variables

Нажмите **"Advanced"** → **"Add Environment Variable"** и добавьте:

```
DB_HOST=pg4.sweb.ru
DB_PORT=5433
DB_USER=headcorne_test
DB_PASSWORD=Ss8SRGP5TH3W6J@L
DB_NAME=headcorne_test
PLANFIX_API_URL=https://megamindru.planfix.ru/rest/
PLANFIX_API_TOKEN=3325457cab2f1a9b69b3c9191eeadc82
SECRET_KEY=ваш-случайный-секретный-ключ-минимум-32-символа
DEBUG=False
CORS_ORIGINS=*
```

**Как сгенерировать SECRET_KEY:**
- Можно использовать: `openssl rand -hex 32` (в Git Bash или WSL)
- Или онлайн генератор: https://randomkeygen.com/
- Или просто длинную случайную строку (минимум 32 символа)

**Примечание:** `CORS_ORIGINS=*` разрешает все домены. После деплоя frontend замените на конкретный URL.

### 3.4. Создайте сервис

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (займет 5-10 минут)
3. Дождитесь сообщения **"Your service is live"**
4. Скопируйте URL (например: `https://dashboard-backend-xxxx.onrender.com`)

**⚠️ ВАЖНО:** Сохраните этот URL! Он понадобится для frontend.

---

## 🎯 Шаг 4: Деплой Frontend (React)

### 4.1. Создайте Static Site

1. В Render Dashboard нажмите **"New +"** → **"Static Site"**
2. Выберите репозиторий: **`evgengiga/dashbord`**
3. Нажмите **"Connect"**

### 4.2. Настройте Frontend

Заполните следующие поля:

- **Name:** `dashboard-frontend` (или любое другое имя)
- **Branch:** `main`
- **Root Directory:** `frontend` ⚠️ **ВАЖНО!**
- **Build Command:** `npm install && npm run build`
- **Publish Directory:** `dist`
- **Plan:** `Free`

### 4.3. Добавьте Environment Variable

Нажмите **"Add Environment Variable"** и добавьте:

```
VITE_API_URL=https://ваш-backend-url.onrender.com/api
```

**⚠️ ВАЖНО:** Замените `ваш-backend-url.onrender.com` на реальный URL вашего backend из шага 3.4!

Например:
```
VITE_API_URL=https://dashboard-backend-xxxx.onrender.com/api
```

### 4.4. Создайте Static Site

1. Нажмите **"Create Static Site"**
2. Render начнет деплой (займет 3-5 минут)
3. Дождитесь сообщения **"Your site is live"**
4. Скопируйте URL (например: `https://dashboard-frontend-xxxx.onrender.com`)

**⚠️ ВАЖНО:** Сохраните этот URL! Это ваш финальный URL дашборда.

---

## 🎯 Шаг 5: Обновите CORS в Backend

### 5.1. Обновите CORS_ORIGINS

1. Вернитесь в Render Dashboard → ваш **backend service**
2. Откройте **"Environment"** → **"Environment Variables"**
3. Найдите `CORS_ORIGINS`
4. Измените значение на URL вашего frontend:

```
CORS_ORIGINS=https://dashboard-frontend-xxxx.onrender.com
```

**Или если хотите разрешить несколько доменов:**
```
CORS_ORIGINS=https://dashboard-frontend-xxxx.onrender.com,https://your-custom-domain.com
```

### 5.2. Передеплойте Backend

1. В Render Dashboard → ваш **backend service**
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Дождитесь завершения деплоя (3-5 минут)

---

## 🎯 Шаг 6: Примените миграции базы данных

### 6.1. Подключитесь к базе данных

Вам нужно выполнить SQL миграцию для создания таблицы `users`:

**Файл:** `backend/migrations/001_create_users_table.sql`

### 6.2. Варианты выполнения миграции:

#### Вариант A: Через pgAdmin (рекомендуется)

1. Откройте pgAdmin
2. Подключитесь к серверу `pg4.sweb.ru:5433`
3. Откройте базу данных `headcorne_test`
4. Откройте **Query Tool**
5. Скопируйте содержимое файла `backend/migrations/001_create_users_table.sql`
6. Вставьте в Query Tool и выполните (F5)

#### Вариант B: Через Python скрипт

1. Откройте терминал на вашем компьютере
2. Перейдите в папку проекта:
   ```bash
   cd C:\Users\dante\OneDrive\Desktop\test-cursor
   ```
3. Запустите скрипт:
   ```bash
   python backend/apply_migration.py
   ```

**Подробные инструкции:** см. `backend/migrations/README.md`

---

## ✅ Шаг 7: Проверка работы

### 7.1. Откройте дашборд

1. Откройте URL вашего frontend (из шага 4.4)
2. Должна открыться страница входа

### 7.2. Протестируйте вход

1. Введите email пользователя из Planfix (например: `svetlana@megamindru.planfix.ru`)
2. Введите пароль (или создайте новый при первом входе)
3. Должен открыться дашборд с данными

### 7.3. Проверьте логи (если что-то не работает)

1. В Render Dashboard → ваш **backend service**
2. Откройте вкладку **"Logs"**
3. Проверьте ошибки

---

## 🔧 Дополнительные настройки

### Настройка кастомного домена (опционально)

1. В Render Dashboard → ваш **frontend service**
2. Откройте **"Settings"** → **"Custom Domain"**
3. Добавьте ваш домен
4. Следуйте инструкциям для настройки DNS

### Обновление кода (после изменений)

1. Закоммитьте изменения в GitHub:
   ```bash
   git add -A
   git commit -m "Update dashboard"
   git push origin main
   ```

2. Render автоматически задеплоит изменения (через 1-2 минуты)

3. Или вручную: Render Dashboard → сервис → **"Manual Deploy"** → **"Deploy latest commit"**

---

## ⚠️ Важные замечания

### Бесплатный тариф Render

- **Backend "засыпает"** после 15 минут неактивности
- Первый запрос после "засыпания" может занять **30-60 секунд** (пока сервис "просыпается")
- Это нормально для бесплатного тарифа

### Если нужна стабильность

- Перейдите на **Starter план** ($7/мес) для backend
- Или используйте **Railway** (есть бесплатный тариф с $5 кредитами)

### Безопасность

- **НЕ коммитьте** секретные ключи в GitHub
- Используйте **Environment Variables** в Render
- Регулярно обновляйте `SECRET_KEY`

---

## 🆘 Решение проблем

### ❌ "Database connection failed"

**Проблема:** Backend не может подключиться к PostgreSQL

**Решение:**
1. Проверьте, что `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` правильно указаны в Environment Variables
2. Убедитесь, что PostgreSQL `pg4.sweb.ru` доступен из интернета
3. Возможно, нужно добавить IP Render в whitelist вашего PostgreSQL

### ❌ CORS ошибки в браузере

**Проблема:** Frontend не может подключиться к backend

**Решение:**
1. Проверьте, что `CORS_ORIGINS` в backend содержит URL вашего frontend
2. Убедитесь, что `VITE_API_URL` в frontend указывает на правильный backend URL
3. Передеплойте backend после изменения `CORS_ORIGINS`

### ❌ "User not found in Planfix"

**Проблема:** Пользователь не найден в Planfix

**Решение:**
1. Проверьте, что email существует в Planfix
2. Проверьте логи backend в Render → "Logs"
3. Убедитесь, что `PLANFIX_API_TOKEN` правильный

### ❌ Backend не запускается

**Проблема:** Ошибки при деплое

**Решение:**
1. Проверьте логи в Render → "Logs"
2. Убедитесь, что `Root Directory` = `backend`
3. Проверьте, что `Start Command` правильный: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
4. Убедитесь, что все Environment Variables заполнены

### ❌ Frontend показывает пустую страницу

**Проблема:** Frontend не может загрузить данные

**Решение:**
1. Откройте консоль браузера (F12) → вкладка "Console"
2. Проверьте ошибки
3. Убедитесь, что `VITE_API_URL` правильный
4. Проверьте, что backend работает (откройте `https://ваш-backend-url.onrender.com/docs`)

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Render Dashboard
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все Environment Variables заполнены правильно
4. Проверьте, что миграции базы данных применены

---

## 🎉 Готово!

Теперь ваш дашборд доступен в интернете! Поделитесь ссылкой с коллегами.

**Ваш URL:** `https://dashboard-frontend-xxxx.onrender.com`

---

**Последнее обновление:** 2025-01-XX

