# 🔧 Исправление .htaccess для порта 8002

## Проблема

Backend работает на порту 8002, но `.htaccess` проксирует на другой порт (вероятно, 8001).

## Решение

**Выполни на основном сервере (НЕ в Docker окружении):**

```bash
# Выйди из Docker окружения (если внутри)
exit

# Перейди в папку public_html
cd ~/dashboard.headcorn.pro/public_html

# Посмотри текущий .htaccess
cat .htaccess | grep -A 5 -B 5 "800"
```

**Найди строку с портом и измени на 8002:**

```apache
# Должно быть что-то вроде:
ProxyPass /api http://127.0.0.1:8001/api
ProxyPassReverse /api http://127.0.0.1:8001/api

# Измени на:
ProxyPass /api http://127.0.0.1:8002/api
ProxyPassReverse /api http://127.0.0.1:8002/api
```

**Или отредактируй файл:**

```bash
nano .htaccess
```

**Найди строки с портом и измени на 8002, затем сохрани:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## Альтернатива: Запустить backend на порту 8001

**Если не хочешь менять .htaccess, запусти backend на порту 8001:**

```bash
# Войди в Docker окружение
ssh localhost -p222

# Перейди в папку
cd ~/dashboard.headcorn.pro/backend

# Останови процесс на 8002 (если запущен)
pkill -f uvicorn

# Запусти на порту 8001
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 > backend.log 2>&1 &
```

---

## Проверка

**После исправления проверь:**

```bash
# Проверь, что backend работает
curl http://127.0.0.1:8002/api/health

# Или если запустил на 8001:
curl http://127.0.0.1:8001/api/health
```

**Затем обнови страницу в браузере** и попробуй войти снова.




