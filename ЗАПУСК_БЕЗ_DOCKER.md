# 🚀 Запуск без Docker (самый простой способ)

## Проблема с Docker?

Если Docker Desktop не запускается или долго грузится - не проблема!

## ✅ Решение: Запуск через батник

### Вариант 1: Автоматический запуск (РЕКОМЕНДУЮ)

Просто **дважды кликните** на файл:

```
start_all.bat
```

Откроются 2 окна:
- Окно 1: Backend (Python)
- Окно 2: Frontend (React)

Через 10-15 секунд автоматически откроется браузер с http://localhost:5173

**Готово!** 🎉

### Вариант 2: Ручной запуск

**Терминал 1 (Backend):**
```powershell
cd C:\Users\dante\OneDrive\Desktop\test-cursor
.\start_backend.bat
```

**Терминал 2 (Frontend):**
```powershell
cd C:\Users\dante\OneDrive\Desktop\test-cursor
.\start_frontend.bat
```

---

## ❓ А что с Docker?

### Если хотите использовать Docker:

1. **Запустите Docker Desktop** (иконка кита)
2. **Дождитесь запуска** (1-2 минуты)
3. **Выполните:**

```powershell
cd C:\Users\dante\OneDrive\Desktop\test-cursor
docker-compose up --build -d
```

4. **Откройте:** http://localhost

---

## 🔧 Если что-то не работает

### Backend не запускается:

```powershell
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

### Frontend не запускается:

```powershell
cd frontend
npm install
npm run dev
```

---

## 📊 Проверка работы

- ✅ Backend: http://localhost:8000/api/health
- ✅ Frontend: http://localhost:5173
- ✅ API Docs: http://localhost:8000/api/docs

---

## 🎯 Мой совет

**Для локальной разработки:**  
→ Используйте `start_all.bat` (проще и быстрее)

**Для production/сервера:**  
→ Используйте Docker или облачный деплой (Railway/Render)

---

Нужна помощь? Спрашивайте! 🚀

