# 🐳 Docker Деплой - Dashboard Service

## Что это дает?

- ✅ **Одна команда** для запуска всего приложения
- ✅ **Не нужно** устанавливать Python, Node.js, зависимости
- ✅ **Работает везде** где есть Docker (Windows, Linux, Mac, облако)
- ✅ **Автоматический перезапуск** при падении
- ✅ **Production-ready** конфигурация с Nginx

---

## Вариант 1: Локальный Docker (на вашем компьютере)

### Шаг 1: Установите Docker Desktop

**Windows:**
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop/
2. Установите и запустите
3. Дождитесь, пока Docker запустится (иконка в трее)

### Шаг 2: Запустите приложение

Откройте PowerShell в папке проекта:

```bash
cd C:\Users\dante\OneDrive\Desktop\test-cursor
docker-compose up --build
```

**Готово!** Приложение будет доступно на:
- 🌐 Frontend: http://localhost
- 🔧 Backend API: http://localhost:8000/api/docs

### Остановить:
```bash
docker-compose down
```

### Запустить в фоновом режиме:
```bash
docker-compose up -d
```

### Посмотреть логи:
```bash
docker-compose logs -f
```

---

## Вариант 2: Деплой на свой сервер (VPS)

Если у вас есть VPS (облачный сервер), например на:
- DigitalOcean
- AWS EC2
- Yandex Cloud
- Selectel
- Timeweb

### На сервере:

```bash
# 1. Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 2. Установите Docker Compose
sudo apt install docker-compose

# 3. Загрузите проект (через git или scp)
git clone <your-repo> dashboard
cd dashboard

# 4. Запустите
docker-compose up -d

# 5. Приложение доступно на IP сервера
# http://ваш-ip-адрес
```

### Настройка домена:

Если у вас есть домен (например, dashboard.yourcompany.ru):

1. Укажите A-запись домена на IP сервера
2. Установите Certbot для SSL:

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d dashboard.yourcompany.ru
```

---

## Вариант 3: Облачные платформы (БЕЗ своего сервера)

### 🚂 Railway (САМЫЙ ПРОСТОЙ)

**Преимущества:**
- ✅ Бесплатный тариф ($5 в месяц бесплатных ресурсов)
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Логи и мониторинг

**Шаги:**

1. **Зарегистрируйтесь:** https://railway.app
2. **Загрузите проект на GitHub** (создайте приватный репозиторий)
3. **В Railway:**
   - New Project → Deploy from GitHub
   - Выберите ваш репозиторий
   - Railway автоматически определит Docker
4. **Настройте переменные окружения** (в Railway Dashboard)
5. **Получите URL:** `https://ваш-проект.railway.app`

### 🎨 Render

**Преимущества:**
- ✅ Бесплатный тариф
- ✅ Простая настройка
- ✅ HTTPS автоматически

**Шаги:**

1. **Зарегистрируйтесь:** https://render.com
2. **Backend:**
   - New → Web Service
   - Connect GitHub repo
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
3. **Frontend:**
   - New → Static Site
   - Build Command: `npm install && npm run build`
   - Publish Directory: `dist`

### ⚡ Vercel + Render

**Backend на Render, Frontend на Vercel**

Vercel идеально для React:
1. Зарегистрируйтесь: https://vercel.com
2. Import Git Repository
3. Framework: Vite
4. Deploy

Настройте в `frontend/.env`:
```env
VITE_API_URL=https://your-backend.onrender.com/api
```

---

## Обновление конфигурации для облака

### Backend: Обновите CORS

В `backend/.env` или переменных окружения платформы:

```env
CORS_ORIGINS=https://your-frontend-url.vercel.app,https://your-domain.com
```

### Frontend: Укажите API URL

В `frontend/src/services/api.js` замените:

```javascript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api'
```

И создайте `frontend/.env.production`:

```env
VITE_API_URL=https://your-backend-url.railway.app/api
```

---

## Сравнение вариантов

| Вариант | Сложность | Стоимость | Время деплоя |
|---------|-----------|-----------|--------------|
| **Docker локально** | Легко | Бесплатно | 5 минут |
| **Свой VPS + Docker** | Средне | ~$5-10/мес | 30 минут |
| **Railway** | Очень легко | $5 бесплатно | 10 минут |
| **Render** | Легко | Бесплатно (с лимитами) | 15 минут |
| **Vercel + Render** | Легко | Бесплатно | 20 минут |

---

## Мои рекомендации

### Для тестирования:
1. **Docker локально** - запустите за 5 минут, не нужна настройка

### Для production (~20 пользователей):
1. **Railway** - самый простой вариант, все в одном месте
2. **Или VPS** (если хотите полный контроль)

### Бюджетный вариант:
1. **Render** (backend) + **Vercel** (frontend) = полностью бесплатно

---

## Готовые команды для быстрого старта

### Docker (локально):
```bash
docker-compose up -d
# Открыть: http://localhost
```

### Railway:
```bash
# 1. Установите Railway CLI
npm i -g @railway/cli

# 2. Логин
railway login

# 3. Инициализация
railway init

# 4. Деплой
railway up
```

---

## Проверка работы

После деплоя проверьте:

1. **Backend health:**
   - Откройте: `https://your-backend-url/api/health`
   - Должен вернуть: `{"status": "healthy"}`

2. **Frontend:**
   - Откройте главную страницу
   - Попробуйте войти

3. **API docs:**
   - `https://your-backend-url/api/docs`

---

## Что дальше?

1. ✅ Выберите вариант деплоя
2. ✅ Запустите по инструкции
3. ✅ Добавьте свои SQL-запросы
4. ✅ Поделитесь ссылкой с коллегами

---

## Нужна помощь?

Скажите, какой вариант вы выбрали, и я помогу с настройкой!

- 🐳 **Docker локально** - самый быстрый старт
- 🚂 **Railway** - самый простой облачный вариант
- 💰 **Бесплатный** - Render + Vercel




