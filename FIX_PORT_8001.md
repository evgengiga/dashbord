# 🔧 Решение: Порт 8001 уже занят

## Проблема

Порт 8001 уже используется другим процессом. Нужно либо освободить порт, либо использовать другой.

---

## Решение 1: Освободить порт 8001

**Внутри Docker окружения выполни:**

```bash
# 1. Найди процесс, который занимает порт 8001
ss -tuln | grep 8001
lsof -i :8001 2>/dev/null

# 2. Найди PID процесса
fuser 8001/tcp 2>/dev/null

# 3. Убей процесс (замени <PID> на реальный PID)
kill -9 <PID>

# Или убей все процессы uvicorn на порту 8001
pkill -f "uvicorn.*8001"
```

**Затем запусти backend заново:**

```bash
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 > backend.log 2>&1 &
```

---

## Решение 2: Использовать другой порт (8002)

**Если не можешь освободить порт 8001, используй 8002:**

```bash
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8002 > backend.log 2>&1 &
```

**Проверь:**

```bash
sleep 3
ps aux | grep uvicorn | grep -v grep
curl http://127.0.0.1:8002/api/health
```

**⚠️ ВАЖНО:** Если используешь порт 8002, нужно будет обновить `.htaccess` на сервере, чтобы он проксировал на порт 8002 вместо 8001.

---

## Решение 3: Найти и перезапустить старый процесс

**Может быть, старый backend все еще работает:**

```bash
# Найди все процессы uvicorn
ps aux | grep uvicorn | grep -v grep

# Если найдешь процесс на порту 8001, убей его
kill -9 <PID>

# Или убей все процессы uvicorn
pkill -f uvicorn

# Подожди немного
sleep 2

# Запусти заново
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 > backend.log 2>&1 &
```

---

## Проверка после исправления

**Выполни:**

```bash
# 1. Проверь, что процесс запустился
ps aux | grep uvicorn | grep -v grep

# 2. Проверь порт
ss -tuln | grep 8001

# 3. Проверь health endpoint
curl http://127.0.0.1:8001/api/health
```

**Ожидаемый результат:**
- Процесс uvicorn запущен
- Порт 8001 слушает
- Health endpoint возвращает JSON

---

## Если все еще не работает

**Запусти в интерактивном режиме, чтобы увидеть ошибки:**

```bash
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8001
```

**Пришли вывод команды.**




