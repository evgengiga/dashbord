# 🚀 Запуск Backend прямо сейчас

## Проблема

Backend не запущен - процесс uvicorn не найден.

## Решение

**Внутри Docker окружения (где ты сейчас) выполни:**

```bash
cd ~/dashboard.headcorn.pro/backend

# 1. Активируй venv
source venv/bin/activate

# 2. Запусти backend на порту 8002
nohup uvicorn app.main:app --host 0.0.0.0 --port 8002 > backend.log 2>&1 &

# 3. Проверь, что запустилось
sleep 3
ps aux | grep uvicorn | grep -v grep

# 4. Проверь доступность
curl http://127.0.0.1:8002/api/health
```

**Если процесс запустился, но curl все еще не работает**, проверь логи:

```bash
tail -30 backend.log
```

---

## Если не запускается

**Запусти в интерактивном режиме, чтобы увидеть ошибки:**

```bash
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8002
```

**Пришли вывод команды.**




