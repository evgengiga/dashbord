# 🚀 Деплой на Railway - Пошаговая инструкция

## Шаг 1: Подготовка GitHub репозитория

### 1.1. Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"New repository"** (зеленая кнопка)
3. **Repository name:** `dashboard-service` (или любое другое имя)
4. **Private/Public:** выберите **Private** (чтобы никто не видел ваши данные)
5. **НЕ** ставьте галочки "Add README", "Add .gitignore"
6. Нажмите **"Create repository"**

### 1.2. Загрузите проект на GitHub

Откройте **PowerShell** или **CMD** в папке проекта:

```bash
cd C:\Users\dante\OneDrive\Desktop\test-cursor
```

Выполните команды **по очереди**:

```bash
# Инициализируем git
git init

# Добавляем все файлы
git add .

# Создаем первый коммит
git commit -m "Initial commit - Dashboard Service"

# Подключаем к вашему репозиторию (ЗАМЕНИТЕ на ваш URL!)
git remote add origin https://github.com/ВАШ-USERNAME/dashboard-service.git

# Загружаем на GitHub
git branch -M main
git push -u origin main
```

**ВАЖНО:** Замените `ВАШ-USERNAME/dashboard-service` на реальный URL вашего репозитория!

---

## Шаг 2: Регистрация на Railway

1. Откройте https://railway.app
2. Нажмите **"Start a New Project"** или **"Login"**
3. Выберите **"Login with GitHub"**
4. Авторизуйте Railway (разрешите доступ к репозиториям)

---

## Шаг 3: Создание проекта на Railway

### 3.1. Создайте новый проект

1. В Railway Dashboard нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий **`dashboard-service`**
4. Railway автоматически начнет деплой

### 3.2. Настройте переменные окружения

1. Кликните на ваш проект (должен появиться в Dashboard)
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"+ New Variable"**

**Добавьте следующие переменные (ВАЖНО - каждую отдельно!):**

```
DB_HOST=pg4.sweb.ru
DB_PORT=5433
DB_USER=headcorne_test
DB_PASSWORD=Ss8SRGP5TH3W6J@L
DB_NAME=headcorne_test

PLANFIX_API_URL=https://megamindru.planfix.ru/rest/
PLANFIX_API_TOKEN=3325457cab2f1a9b69b3c9191eeadc82

SECRET_KEY=railway-production-secret-key-super-secure-32-chars-min
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

APP_NAME=Dashboard Service
DEBUG=False
```

**Для CORS_ORIGINS пока оставьте пустым** - добавим после получения URL.

### 3.3. Получите URL вашего сервиса

1. В Railway проект должен задеплоиться (~2-5 минут)
2. Перейдите на вкладку **"Settings"**
3. Найдите секцию **"Domains"**
4. Нажмите **"Generate Domain"**
5. Скопируйте URL (например: `https://dashboard-service-production-xxxx.up.railway.app`)

### 3.4. Обновите CORS_ORIGINS

1. Вернитесь на вкладку **"Variables"**
2. Добавьте переменную:

```
CORS_ORIGINS=https://ваш-url.up.railway.app
```

Замените на ваш реальный URL!

3. Railway автоматически передеплоит проект

---

## Шаг 4: Проверка работы

### 4.1. Откройте ваш сайт

Откройте URL из Railway (например: `https://dashboard-service-production-xxxx.up.railway.app`)

Вы должны увидеть страницу входа! 🎉

### 4.2. Проверьте API

Откройте: `https://ваш-url.up.railway.app/api/health`

Должен вернуть:
```json
{
  "status": "healthy",
  "database": "connected"
}
```

### 4.3. Войдите в систему

1. На главной странице введите email из Planfix
2. Нажмите "Войти"
3. Вы должны увидеть дашборд!

---

## 🎯 Краткая версия для быстрого старта

```bash
# 1. В папке проекта
cd C:\Users\dante\OneDrive\Desktop\test-cursor
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ВАШ-USERNAME/dashboard-service.git
git push -u origin main

# 2. Railway.app
# - Login with GitHub
# - New Project → Deploy from GitHub
# - Выбрать репозиторий
# - Добавить переменные окружения (см. выше)
# - Generate Domain
# - Готово!
```

---

## 🐛 Устранение проблем

### ❌ Build Failed

**Проверьте логи:**
1. Railway Dashboard → ваш проект
2. Вкладка **"Deployments"**
3. Кликните на последний деплой
4. Смотрите логи - там будет ошибка

**Частые причины:**
- Не хватает зависимостей в `requirements.txt`
- Ошибка в коде Python
- Неправильные пути

### ❌ Application Error

**Проверьте:**
1. Все переменные окружения добавлены
2. База данных доступна (попробуйте подключиться с другого компьютера)
3. Planfix API токен правильный

### ❌ CORS Error в браузере

**Решение:**
Добавьте/обновите `CORS_ORIGINS` в Railway Variables с вашим URL.

---

## 📝 Что дальше?

1. ✅ Добавьте свои SQL-запросы в `backend/app/services/dashboard_service.py`
2. ✅ Загрузите изменения на GitHub:
   ```bash
   git add .
   git commit -m "Added SQL queries"
   git push
   ```
3. ✅ Railway автоматически передеплоит изменения!

---

## 💰 Стоимость

Railway дает **$5 бесплатно каждый месяц**. Для ~20 пользователей этого более чем достаточно!

---

## 🎉 Готово!

Теперь у вас есть рабочий веб-сервис, доступный из любой точки мира!

**URL:** `https://ваш-проект.up.railway.app`

Поделитесь ссылкой с коллегами! 🚀

