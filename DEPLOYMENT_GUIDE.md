# 🚀 Полное руководство по развертыванию и работе с проектом

## 📋 Оглавление
1. [Текущее состояние проекта](#текущее-состояние-проекта)
2. [Развертывание на Render (рекомендуется)](#развертывание-на-render)
3. [Привязка собственного домена](#привязка-домена)
4. [Как деплоить изменения](#деплой-изменений)
5. [Мониторинг и логи](#мониторинг-и-логи)
6. [Troubleshooting](#troubleshooting)

---

## 🎯 Текущее состояние проекта

### Что уже есть:
- ✅ **Backend (FastAPI)** - готов к развертыванию
- ✅ **Frontend (React + Vite)** - готов к развертыванию
- ✅ **База данных PostgreSQL** - уже настроена на `pg4.sweb.ru`
- ✅ **GitHub репозиторий** - `https://github.com/evgengiga/dashbord.git`
- ✅ **Интеграция с Planfix API** - настроена

### Структура проекта:
```
test-cursor/
├── backend/          # FastAPI приложение
├── frontend/         # React приложение
├── docker-compose.yml
└── render.yaml       # Конфигурация для Render
```

---

## 🌐 Развертывание на Render

### Почему Render?
- ✅ Бесплатный тариф для статических сайтов
- ✅ Простая интеграция с GitHub
- ✅ Автоматический HTTPS
- ✅ Легкая привязка домена

### Шаг 1: Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (это важно для автодеплоя!)
4. Подтвердите email

### Шаг 2: Развертывание Backend (Web Service)

1. **В Render Dashboard нажмите "New +" → "Web Service"**

2. **Подключите GitHub репозиторий:**
   - Выберите репозиторий: `evgengiga/dashbord`
   - Нажмите "Connect"

3. **Настройте сервис:**
   ```
   Name: dashbord-backend
   Region: Frankfurt (EU Central) или Singapore (ближайший к России)
   Branch: main
   Root Directory: backend
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

4. **Instance Type:**
   - Выберите: **Free** (первые 750 часов бесплатно)
   - ⚠️ Важно: Free tier "засыпает" через 15 мин бездействия (первый запрос будет медленным)

5. **Environment Variables** (нажмите "Add Environment Variable"):
   ```
   DB_HOST=pg4.sweb.ru
   DB_PORT=5433
   DB_USER=headcorne_test
   DB_PASSWORD=Ss8SRGP5TH3W6J%40L
   DB_NAME=headcorne_test
   
   PLANFIX_API_URL=https://megamindru.planfix.ru/rest/
   PLANFIX_TOKEN=3325457cab2f1a9b69b3c9191eeadc82
   
   XML_API_KEY=f6d50e651c89858b9bad67a482b3ad64
   XML_TOKEN=2f064a30c8530668cd4e01176be1fb9d
   XML_PRIVATE_KEY=41e92c92001fb0197494520a53cb3cd6
   PLANFIX_ACCOUNT=megamindru
   
   SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars
   DEBUG=False
   CORS_ORIGINS=https://your-frontend-url.onrender.com
   ```

6. **Нажмите "Create Web Service"**

7. **После успешного деплоя:**
   - Скопируйте URL (например: `https://dashbord-backend.onrender.com`)
   - ✅ Проверьте: откройте `https://dashbord-backend.onrender.com/docs` - должна открыться Swagger UI

### Шаг 3: Развертывание Frontend (Static Site)

1. **В Render Dashboard нажмите "New +" → "Static Site"**

2. **Подключите тот же GitHub репозиторий:**
   - Выберите: `evgengiga/dashbord`
   - Нажмите "Connect"

3. **Настройте сайт:**
   ```
   Name: dashbord-frontend
   Branch: main
   Root Directory: frontend
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

4. **Environment Variables:**
   ```
   VITE_API_URL=https://dashbord-backend.onrender.com
   ```
   (⚠️ Замените на ваш реальный URL backend из Шага 2!)

5. **Нажмите "Create Static Site"**

6. **После деплоя:**
   - Скопируйте URL frontend (например: `https://dashbord-frontend.onrender.com`)

### Шаг 4: Обновите CORS на Backend

1. Вернитесь в **настройки Backend Web Service**
2. Найдите переменную `CORS_ORIGINS`
3. **Обновите значение:**
   ```
   CORS_ORIGINS=https://dashbord-frontend.onrender.com
   ```
   (используйте реальный URL вашего frontend!)

4. Сохраните изменения
5. Backend автоматически перезапустится

### ✅ Проверка работоспособности

1. Откройте frontend URL в браузере
2. Попробуйте залогиниться с email из Planfix
3. Проверьте, что данные загружаются

---

## 🌍 Привязка собственного домена

### Вариант 1: Привязка к Frontend (рекомендуется)

1. **В настройках Static Site (Frontend):**
   - Перейдите на вкладку **"Settings"**
   - Найдите секцию **"Custom Domain"**
   - Нажмите **"Add Custom Domain"**

2. **Введите ваш домен:**
   ```
   dashboard.yourdomain.com
   ```

3. **Настройте DNS у вашего регистратора:**
   
   **Если это поддомен (рекомендуется):**
   ```
   Type: CNAME
   Name: dashboard (или любое другое имя)
   Value: dashbord-frontend.onrender.com
   TTL: 3600 (или auto)
   ```

   **Если это корневой домен (yourdomain.com):**
   ```
   Type: A
   Name: @ (или пусто)
   Value: [IP адрес, который покажет Render]
   TTL: 3600
   ```

4. **Дождитесь распространения DNS** (5-30 минут)

5. **Render автоматически настроит HTTPS** через Let's Encrypt

6. **Обновите CORS_ORIGINS на Backend:**
   ```
   CORS_ORIGINS=https://dashboard.yourdomain.com,https://dashbord-frontend.onrender.com
   ```
   (можно указать несколько через запятую!)

### Вариант 2: Привязка к Backend

Аналогично, но в настройках **Web Service (Backend)**:
- Custom Domain: `api.yourdomain.com`
- DNS: `CNAME → dashbord-backend.onrender.com`
- Обновите `VITE_API_URL` во Frontend на новый домен

### Популярные регистраторы доменов:

- **REG.RU** - российский регистратор
- **Namecheap** - удобный и недорогой
- **Cloudflare** - бесплатные DNS + CDN
- **Timeweb** - российский хостинг

---

## 🔄 Деплой изменений (самое важное!)

### Автоматический деплой (рекомендуется)

**✨ Render уже настроен на автодеплой!** Просто:

1. **Вносите изменения в код локально**
2. **Коммитите и пушите в GitHub:**
   ```bash
   git add .
   git commit -m "Ваше описание изменений"
   git push origin main
   ```

3. **Render автоматически:**
   - Обнаружит изменения в GitHub
   - Запустит новую сборку
   - Задеплоит изменения
   - **Время деплоя:** 2-5 минут для backend, 3-7 минут для frontend

4. **Отслеживайте прогресс:**
   - В Render Dashboard увидите статус: **"Deploying..."**
   - Можете смотреть логи в реальном времени

### Ручной деплой

Если нужно задеплоить без изменений в коде (например, изменили только переменные окружения):

1. Зайдите в настройки сервиса (Backend или Frontend)
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Или: **"Clear build cache & deploy"** (если что-то сломалось)

### Стратегия работы с кодом

```bash
# 1. Вносите изменения локально
# 2. Тестируйте локально (если нужно)

# 3. Коммитите с понятным описанием
git add .
git commit -m "Fix: исправлена сортировка в таблице просроченных задач"

# 4. Пушите в main (или создайте отдельную ветку для тестов)
git push origin main

# 5. Ждите автодеплоя на Render (2-5 мин)

# 6. Проверяйте на продакшене
```

### Работа с ветками (для продвинутых)

```bash
# Создайте ветку для разработки
git checkout -b feature/new-table

# Вносите изменения
git add .
git commit -m "Add new data table"

# Пушите ветку
git push origin feature/new-table

# После тестов - мержите в main
git checkout main
git merge feature/new-table
git push origin main
```

---

## 📊 Мониторинг и логи

### Просмотр логов Backend

1. Зайдите в **Web Service (Backend)**
2. Перейдите на вкладку **"Logs"**
3. Увидите логи в реальном времени:
   ```
   🔍 Executing conversions query for user: 'Александра Борисова'
   ✅ Found 3 conversions
   ```

### Просмотр метрик

1. Вкладка **"Metrics"**
2. Здесь увидите:
   - CPU usage
   - Memory usage
   - Request count
   - Response time

### Алерты

Render автоматически пришлет email если:
- ❌ Деплой упал
- ❌ Сервис недоступен
- ⚠️ Закончилась квота

---

## 🔧 Troubleshooting

### ❌ Backend не стартует

**Проблема:** Ошибка при деплое backend

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь, что все Environment Variables указаны правильно
3. Проверьте `requirements.txt` - все ли библиотеки указаны
4. Попробуйте **"Clear build cache & deploy"**

**Частые ошибки:**
```
ModuleNotFoundError: No module named 'uvicorn'
→ Добавьте uvicorn в requirements.txt

could not translate host name
→ Проверьте DB_HOST и DB_PASSWORD (спецсимволы нужно экранировать %40 для @)

Access denied, unknown token
→ Проверьте XML_TOKEN и XML_API_KEY
```

### ❌ Frontend не подключается к Backend

**Проблема:** `ERR_CONNECTION_REFUSED` или CORS error

**Решение:**
1. Проверьте `VITE_API_URL` во Frontend - правильный ли URL backend?
2. Проверьте `CORS_ORIGINS` на Backend - добавлен ли URL frontend?
3. Убедитесь, что backend успешно запущен (проверьте `/docs`)
4. Попробуйте перезапустить оба сервиса

### ❌ База данных не подключается

**Проблема:** Connection timeout или authentication failed

**Решение:**
1. Проверьте, что порт указан **5433**, а не 5432
2. Проверьте пароль: символ `@` должен быть закодирован как `%40`
3. Проверьте, что IP Render не заблокирован на хостинге БД
4. Попробуйте подключиться к БД из локальной машины для проверки

### ❌ Planfix API не находит пользователя

**Проблема:** `404: Пользователь с email ... не найден`

**Решение:**
1. Проверьте логи - какой email передается
2. Убедитесь, что пользователь существует в Planfix
3. Проверьте XML_TOKEN и XML_API_KEY
4. Проверьте PLANFIX_ACCOUNT (должен быть `megamindru`)

### ⚠️ Free tier "засыпает"

**Проблема:** Первый запрос после 15 минут бездействия медленный

**Решение:**
- Это нормальное поведение Free tier на Render
- Первый запрос "разбудит" сервис (~30-60 сек)
- Последующие запросы будут быстрыми

**Как избежать (платные опции):**
1. Обновитесь до **Starter Plan** ($7/месяц) - сервис не спит
2. Или используйте **UptimeRobot** для периодических пингов (но это против ToS Render)

### 🔍 Отладка локально

Если что-то не работает на продакшене, но нужно проверить локально:

```bash
# Backend
cd backend
pip install -r requirements.txt
# Создайте .env файл с теми же переменными, что на Render
uvicorn app.main:app --reload

# Frontend (в новом терминале)
cd frontend
npm install
# Создайте .env файл: VITE_API_URL=http://localhost:8000
npm run dev
```

---

## 📝 Чек-лист перед продакшеном

- [ ] Все Environment Variables настроены правильно
- [ ] Backend успешно запустился (проверить `/docs`)
- [ ] Frontend собрался без ошибок
- [ ] CORS настроен (frontend URL в `CORS_ORIGINS`)
- [ ] Проверен вход по email из Planfix
- [ ] Проверено отображение данных из БД
- [ ] Логи не показывают критичных ошибок
- [ ] (Опционально) Настроен custom domain
- [ ] (Опционально) Настроены алерты

---

## 🚀 Быстрая справка

### Где что находится:

| Что                  | Где                                            |
|----------------------|------------------------------------------------|
| Backend код          | `backend/`                                     |
| Frontend код         | `frontend/`                                    |
| Backend на Render    | https://dashbord-backend.onrender.com          |
| Frontend на Render   | https://dashbord-frontend.onrender.com         |
| База данных          | pg4.sweb.ru:5433                               |
| GitHub               | https://github.com/evgengiga/dashbord          |
| Swagger UI (API)     | https://dashbord-backend.onrender.com/docs     |

### Основные команды Git:

```bash
# Проверить статус
git status

# Добавить все файлы
git add .

# Закоммитить
git commit -m "Описание изменений"

# Запушить в GitHub (автоматически задеплоит на Render)
git push origin main

# Посмотреть историю
git log --oneline

# Откатить последний коммит (осторожно!)
git reset --hard HEAD~1
git push origin main --force
```

### Полезные ссылки:

- 📘 [Render Docs](https://render.com/docs)
- 📘 [FastAPI Docs](https://fastapi.tiangolo.com/)
- 📘 [React Docs](https://react.dev/)
- 📘 [Planfix API](https://planfix.com/ru/help/XML_API_v1)

---

## 💡 Советы

1. **Всегда коммитьте с понятными сообщениями** - потом будет легче найти изменения
2. **Проверяйте логи на Render** после каждого деплоя - они помогут найти проблемы
3. **Не храните пароли в коде** - используйте Environment Variables
4. **Делайте бэкапы БД** - на всякий случай
5. **Тестируйте локально** перед пушем в main (по возможности)
6. **Используйте ветки** для крупных изменений
7. **Мониторьте метрики** - так вы увидите, если что-то пошло не так

---

## 🎓 Дальнейшее развитие

### Что можно улучшить:

- [ ] Добавить систему аутентификации с паролями (когда понадобится)
- [ ] Настроить CI/CD с автоматическими тестами
- [ ] Добавить кэширование для ускорения запросов
- [ ] Настроить CDN через Cloudflare
- [ ] Добавить логирование в внешний сервис (Sentry, LogRocket)
- [ ] Создать мобильную версию интерфейса
- [ ] Добавить экспорт данных в Excel/PDF
- [ ] Настроить уведомления о просроченных задачах

---

**Удачи с проектом! 🚀**

Если возникнут вопросы - всегда готов помочь! 💪








