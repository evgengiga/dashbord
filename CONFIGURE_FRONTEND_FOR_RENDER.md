# 🔧 Настройка Frontend на Beget для работы с Render Backend

## Что нужно сделать

1. Узнать URL твоего Render backend
2. Создать `.env.production` с этим URL
3. Пересобрать frontend
4. Загрузить на Beget
5. Обновить CORS в Render

---

## ШАГ 1: Узнай URL твоего Render Backend

**В Render Dashboard:**
1. Открой сервис `dashboard-backend`
2. Скопируй URL (типа `https://dashboard-backend.onrender.com`)

**Или проверь в браузере:**
- Открой `https://dashboard-backend.onrender.com/api/health`
- Если возвращает JSON - это правильный URL

---

## ШАГ 2: Создай .env.production

**На своем компьютере:**

1. Открой папку `frontend/`
2. Создай файл `.env.production` (если нет)
3. Добавь:

```env
VITE_API_URL=https://dashboard-backend.onrender.com/api
```

**⚠️ ЗАМЕНИ `dashboard-backend.onrender.com` на свой реальный URL из Render!**

---

## ШАГ 3: Пересобери Frontend

**На своем компьютере:**

```bash
cd frontend
npm install
npm run build
```

**Файлы будут в папке `frontend/dist/`**

---

## ШАГ 4: Загрузи на Beget

**Через FileZilla:**

1. Подключись к серверу
2. Перейди в `~/dashboard.headcorn.pro/public_html/`
3. Удали все старые файлы (или сделай backup)
4. Загрузи ВСЕ файлы из `frontend/dist/` в `public_html/`

---

## ШАГ 5: Обнови CORS в Render

**В Render Dashboard:**

1. Открой сервис `dashboard-backend`
2. Перейди в **Environment**
3. Найди переменную `CORS_ORIGINS`
4. Обнови на:
   ```
   CORS_ORIGINS=https://dashboard.headcorn.pro
   ```
5. Нажми **Save Changes**
6. Render автоматически перезапустит backend

---

## ШАГ 6: Проверка

1. Открой `https://dashboard.headcorn.pro`
2. Попробуй войти
3. Если работает - готово!

---

## Если не работает

### Проблема 1: CORS ошибки

**Решение:** Проверь, что в Render `CORS_ORIGINS` содержит твой домен:
```
CORS_ORIGINS=https://dashboard.headcorn.pro
```

### Проблема 2: 404 на API

**Решение:** 
1. Проверь `.env.production` - правильный ли URL?
2. Пересобери frontend: `npm run build`
3. Загрузи заново на Beget

### Проблема 3: Backend не отвечает

**Решение:** 
1. Проверь в Render Dashboard → Logs
2. Проверь, что backend запущен (статус "Deployed")
3. Проверь URL: `https://dashboard-backend.onrender.com/api/health`

---

**Готово! Если нужна помощь - напиши!**




